---
instructions:
  - name: BCC
    description: Branch on Carry Clear
    cycles:
      - operations:
          - if: C==0
            then:
              alu:
                function: add
                input-a: PC
                input-b: DL
                dst: PC
    group: branch
    opcodes:
      - hex: 90
        mode: relative
