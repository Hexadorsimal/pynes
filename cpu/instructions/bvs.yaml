---
instructions:
  - name: BVS
    description: Branch on Overflow Set
    cycles:
      - operations:
          - if: V==1
            then:
              alu:
                function: add
                input-a: PC
                input-b: DL
                dst: PC
    group: branch
    opcodes:
      - hex: 70
        mode: relative
