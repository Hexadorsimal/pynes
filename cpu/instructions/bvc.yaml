---
instructions:
  - name: BVC
    description: Branch on Overflow Clear
    cycles:
      - operations:
          - if: V==0
            then:
              alu:
                function: add
                input-a: PC
                input-b: DL
                dst: PC
    group: branch
    opcodes:
      - hex: 50
        mode: relative
