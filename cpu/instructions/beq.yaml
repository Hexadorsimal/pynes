---
instructions:
  - name: BEQ
    description: Branch on Result Zero
    cycles:
      - operations:
          - if: Z==1
            then:
              alu:
                function: add
                input-a: PC
                input-b: DL
                dst: PC
    group: branch
    opcodes:
      - hex: F0
        mode: relative
